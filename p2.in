// p2:
{
    int a, b;
    string s;
    s = "[]-abc";
    a = -6;
    b = 8;
    while (a != b) {
        if (a >= b) {
            a = a - b;
        } else {
            b = b - a;
        }
    }
}